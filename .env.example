VITE_API_BASE_URL=http://localhost:8086

# Optional: Dev-only Vite proxy target for /api and /uploads.
# Use this when your backend runs on a different local port (example: Spring profile "local" uses 8087).
# VITE_DEV_PROXY_TARGET=http://localhost:8087
